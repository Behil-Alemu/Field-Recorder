const customStyles = {
	control: (provided) => ({
		...provided,
		backgroundColor: '#E2EAEE',
		borderColor: 'white'
	}),
	singleValue: (provided) => ({
		...provided,
		color: 'black'
	})
};

export default customStyles;
